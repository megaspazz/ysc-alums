<li>
	<%= link_to(user.name , user, :class => ("admin" if user.admin?)) %> 
	<% if (current_user.admin? && !current_user?(user)) && !user.admin? %>
    	| <%= link_to("Deleet", user, :method => :delete, :data => { :confirm => "Are you sure you want to delete #{user.name}?\nYou can't undo this, and they will be deleted forever!" }) %>
      | <%= link_to("Make admin", make_admin_url(user), :data => { :confirm => "Are you sure you want to make #{user.name} an admin?\nIt's not that easy to un-admin/delete an admin!" }) %>
	<% end %>
</li>
