<% provide(:title, 'All Users') %>

<h1>All users</h1>

<%= will_paginate %>
	<ul class="users-list">
		<% @users.each do |user| %>
			<%= render('user_in_list', :user => user) %>
		<% end %>
	</ul>
<%= will_paginate %>
