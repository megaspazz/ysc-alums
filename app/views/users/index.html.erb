<% provide(:title, 'All Users') %>

<h1>All users</h1>

<%= form_tag users_url, :method => :get do %>
  <p>
    
    <%= label_tag :search_distance, "Search within" %>
    <%= text_field_tag :search_distance, params[:search_distance], :class => "has-focus-tooltip", :title => "Default is 50 miles." %>
    
    <%= label_tag :search_location, "miles of" %>
    <%= text_field_tag :search_location, params[:search_location], :class => "has-focus-tooltip", :title => "Enter a city, address, or place (like Beijing University)." %>
    
    <%= submit_tag "Search Near", :name => nil %>
  </p>
<% end %>

<%= will_paginate %>
	<div class="container users-table">
		<% @users.each do |user| %>
		  <div class="container user-row">
			  <%= render('user_in_list', :user => user) %>
			</div>
		<% end %>
	</div>
<%= will_paginate %>
