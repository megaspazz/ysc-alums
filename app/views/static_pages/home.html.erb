<% provide(:title, 'Home') %>

<div class="center hero-unit">
	<h1>Welcome</h1>
	<h2>to the YSC Alumni Directory</h2>
	<% if (!signed_in?) %>
	  <p><%= link_to("Login", login_url, :class => "btn btn-large btn-primary") %></p>
		<p><%= link_to("Register", register_url, :class => "btn btn-large") %></p>
	<% end %>
</div>
